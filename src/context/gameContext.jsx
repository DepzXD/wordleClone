import { createContext } from 'react'

export const BoardState = createContext({})
export const GameState = createContext({})
export const BoardUIContext = createContext({})
export const WordList = createContext({})
